{
  "name": "todo-app",
  "version": "1.0.0",
  "description": "React Node Postgres",
  "main": "index.js",
  "scripts": {
    "build": "if-env TYPE=server && (cd server && npm install) || (cd client && npm install)",
    "server": "node ./server/server.js",
    "client": "cd client && npm start"
  },
  "author": "Bruno Batista Soares",
  "license": "ISC",
  "dependencies": {
    "if-env": "^1.0.4",
    "react": "^18.2.0"
  }
}